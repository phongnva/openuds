all:
  children:
    # ─── Tunnel nodes (run udstunnel service) ───────────────────────────────────
    tunnel_servers:
      hosts:
        uds-tunnel01:
          ansible_host: 103.131.85.186
          keepalived_state: MASTER
          keepalived_priority: 100
        uds-tunnel02:
          ansible_host: CHANGE_ME          # ← điền IP tunnel node 2
          keepalived_state: BACKUP
          keepalived_priority: 90

    # ─── HA layer (HAProxy + Keepalived, same nodes as above) ───────────────────
    haproxy_nodes:
      hosts:
        uds-tunnel01:
        uds-tunnel02:

  vars:
    ansible_user: root
    ansible_ssh_private_key_file: /home/phongnva/Desktop/PhongNVA/openuds/server/deployment/Ansible/phongnva
    ansible_python_interpreter: /usr/bin/python3
